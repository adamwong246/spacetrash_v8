{
  "name": "spacetrash_v8",
  "version": "1.0.0",
  "main": "index.js",
  "author": "adam wong <adamwong246@gmail.com>",
  "license": "MIT",
  "scripts": {
    "nuke": "rm -rf dist/** && rm -rf node_modules && yarn install",
    "build": "esbuild --bundle --format=esm --loader:.html=copy --outdir=dist src/index.tsx src/index.html",
    "dev": "esbuild --serve --bundle --format=esm --loader:.html=copy --outdir=dist src/index.tsx src/index.html"
  },
  "dependencies": {
    "esbuild": "^0.21.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
